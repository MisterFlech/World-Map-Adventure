using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpriteAnimation : MonoBehaviour
{
    [SerializeField] public Sprite[] spritesDown;
    [SerializeField] public Sprite[] spritesLeft;
    [SerializeField] public Sprite[] spritesRight;
    [SerializeField] public Sprite[] spritesUp;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
